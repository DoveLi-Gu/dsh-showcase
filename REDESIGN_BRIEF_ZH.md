# dsh-showcase 中文重设计规范 v2

## 目标

把当前英文开发演示页升级为面向中文用户的高完成度交付证据工具。界面应像一张可操作、可导出的证据海报，而不是营销落地页或普通后台模板。

## 当前问题

1. 绝大多数可见标签为英文，与中文用户场景不符。
2. 主海报和下方区域重复展示五阶段轨道，信息冗余。
3. `Field Signal` 与 `Blue Big Fish` 共用鲸鱼娘主视觉，主题只有外框颜色差异。
4. 蓝色主题仍使用高饱和黄色主按钮，色彩关系突兀。
5. 手机端为了避让文字隐藏了角色脸部，只留下裙摆和鲸尾。
6. 页面标题、状态、指标、操作按钮缺少明确中文层级，整体像原型而不是成品。

## 语言规范

- 产品名、文件路径、Git 引用、命令和代码保持原文。
- 其他用户可见界面全部使用简体中文。
- 主题公开名称：`边境信号`、`蓝色大肥鱼`。
- 五阶段：`提示`、`构建`、`测试`、`捕获`、`交付`。
- 状态：`已验证`、`准备中`、`需要复核`、`执行失败`、`已脱敏`。
- 区段：`交付摘要`、`界面证据`、`变更清单`、`验证回执`、`隐私审查`、`导出证据`。
- 操作：`网页`、`数据`、`海报`、`README 片段`、`发布`。
- 本地声明：`仅在本地生成 / 不会上传`。

## 共用信息架构

1. 顶部工具栏：项目名、Git 范围、生成时间、验证状态、主题切换。
2. 单一主海报：任务目标、状态、唯一一条五阶段轨道、三个核心指标、视口信息、本地声明。
3. 正文证据带：摘要、截图与对比、Diff、测试回执、隐私审查、导出。
4. 删除主海报下方重复的 `operations-rail`。
5. 正文使用全宽分区和细分隔线，不使用嵌套卡片。

## 主题一：边境信号

- 不显示鲸鱼娘图片。
- 原创工业科幻信号系统，不使用“终末地”名称、Logo、角色或素材。
- 背景：冷黑 `#0b0d0e`、矿物灰 `#171a1c`、冷白 `#eef0ea`。
- 唯一强调色：安全黄 `#d8f032`；成功状态可用低饱和薄荷绿。
- 海报采用硬边 12 栏网格、编号、定位刻度、细线、局部反白。
- 排版更加克制和不对称：左侧任务，右侧巨大 `05` 阶段编号与纵向指标。
- 圆角最多 2px，不使用阴影和玻璃效果。

## 主题二：蓝色大肥鱼

- 鲸鱼娘只出现在主海报和导出海报，不重复进入正文工具区。
- “大肥鱼”只是昵称，人物保持正常偏纤细体型。
- 深海蓝 `#061925`、海面蓝 `#173d56`、泡沫白 `#edf8f8`、海沫青 `#65d4c2`，少量珊瑚红用于警告。
- 不再使用黄色作为主题切换的主色。
- 海报右侧完整显示角色脸、紧凑鲸鳍耳、上身和鲸尾；左侧文字不得压到脸或耳鳍。
- 表面可有少量半透明深海面板，但不使用大面积模糊玻璃和发光外阴影。

## 字体与排版

- 中文 UI：`MiSans`, `HarmonyOS Sans SC`, `Noto Sans SC`, `Microsoft YaHei UI`, sans-serif。
- 数据与命令：`JetBrains Mono`, `IBM Plex Mono`, `Cascadia Mono`, monospace。
- 页面主标题桌面不超过 56px，移动端不超过 36px。
- 所有数字使用等宽数字；长任务文本使用 `text-wrap: balance` 或 `pretty`。
- 禁止负字距；小标签可使用正字距。

## 响应式

- 桌面主海报保持约 16:9，内容区最大 1440px。
- 低于 720px 时，主海报改成上下两段，不把所有文字压在背景图上。
- 蓝色主题手机端：上半部为中文任务与指标，下半部为独立角色画面，必须看见脸和耳鳍。
- 五阶段在手机端保持五等分，可使用两字中文；不得横向滚动。
- 任何按钮、标签、标题、Diff 和测试命令不得横向溢出。

## 交互与可访问性

- 保留原生 range、键盘方向键、Home/End 支持。
- 保留主题 localStorage、Diff 文件选择、测试详情展开、隐私状态和导出逻辑。
- 主题切换使用 240ms 遮罩/淡入切换；主海报可通过 React `key={theme}` 触发一次性重播，不改变页面高度。
- 主海报标题、验证状态、阶段和指标按 50-80ms 间隔分批进入。
- **主要表现为常驻氛围特效**，页面停留时仍持续运动，但必须安静、低频、低位移。
- `边境信号` 常驻效果：一条低对比扫描线以 6-9 秒周期缓慢掠过网格；已完成阶段节点交替微弱脉冲；边缘刻度或短数据线以低速平移。禁止高频闪烁、RGB 故障抖动和大面积发光。
- `蓝色大肥鱼` 将角色改为独立绝对定位的 `<img>`，常驻 7-10 秒、位移不超过 8px 的缓慢呼吸/漂浮；背景常驻非常淡的水面光纹移动；最多 5-7 个气泡以不同的 8-14 秒周期上浮；偶发低对比声呐环扩散。
- 正文可保留一个常驻状态信号：当前阶段线缓慢流动，验证状态点以 2.8-4 秒周期呼吸。正文其余区域保持安静，避免整页所有元素同时运动。
- 正文证据区使用 `IntersectionObserver` 在首次进入视口时添加显现状态，必须清理 observer；不得监听 scroll 事件。
- 主题切换、按钮 hover/active 使用 180-260ms，仅动画 `transform` 和 `opacity`。
- 提供明显 focus-visible；遵守 `prefers-reduced-motion`。
- `prefers-reduced-motion: reduce` 时关闭扫描线、漂浮、气泡、分批进入和滚入动画。
- Cover 素材预加载和同步 Blob 下载逻辑不得退化。

## 需修改文件

- `src/App.tsx`
- `src/styles.css`
- `src/core/demo-fixture.ts`
- `index.html`
- 插件本地化阶段再修改 `plugin/` 和测试。

## 验收标准

1. 首屏除产品名、命令和路径外无英文 UI 标签。
2. `边境信号` 主海报完全不出现鲸鱼娘。
3. `蓝色大肥鱼` 主海报能清楚看到角色脸与耳鳍。
4. 页面只出现一次五阶段轨道。
5. 桌面 1600x900 与手机 390x844 均无重叠、横向滚动或文字截断。
6. 页面静置 15 秒时，两主题均能观察到持续但克制的常驻氛围特效；减少动态偏好下保持静态稳定。
7. 现有类型检查、11 项测试与生产构建继续通过。
